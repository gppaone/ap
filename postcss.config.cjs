// postcss.config.cjs

module.exports = {
    plugins: {
      // 1. Loads the Tailwind CSS PostCSS plugin
      tailwindcss: {},
      // 2. Loads the Autoprefixer PostCSS plugin (recommended for browser compatibility)
      autoprefixer: {},
    },
  }